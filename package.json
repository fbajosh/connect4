{
  "name": "connect4-web",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "bash scripts/build_wasm.sh",
    "export:precomputed": "python3 scripts/export_precompute_snapshot.py",
    "predev": "npm run build:wasm",
    "dev": "vite",
    "prebuild": "npm run build:wasm",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.3"
  }
}
